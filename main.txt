class Solution {
    public int[] nextGreaterElement(int[] nums1, int[] nums2) {
        int arr[] = new[nums1.length];
        for(int i = 0;i<arr.length;i++)
        {
            arr[i] = -1
        }
    }
}